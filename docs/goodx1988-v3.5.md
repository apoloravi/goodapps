# GoodX1988 v3.5

Vincula o APK ao cadastro real do Gestor App:

- `app_id=3`
- endpoint `http://multi.painelgood.xyz/api/goodx_config.php`
- chave configurada no painel
- fallback local se o servidor estiver indisponível

O endpoint respondeu `200 OK` com painel, logo, fundo e política de tela ativa. A atualização visual completa será aplicada em uma etapa posterior; esta versão já sincroniza o painel remoto usado no login.
